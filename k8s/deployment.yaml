apiVersion: apps/v1
kind: Deployment

metadata:
  name: quick-note
  namespace: quick-note

spec: 
  replicas: 1

  selector:
    matchLabels:
      app: quick-note
  
  template:
    metadata:
      labels:
        app: quick-note
  
    spec:
      serviceAccountName: quick-note-sa

      containers:
        - name: quick-note
          image: asia-south1-docker.pkg.dev/quck-note/quick-note/quick-note:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: quick-note-secret

            - secretRef:
                name: quick-note-gcp-secret
        
          resources:
            requests:
              memory: "512Mi"
              cpu: "50m"
              
            limits:
              memory: "1Gi"
              cpu: "500m"

          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000

            initialDelaySeconds: 60

            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5

          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000

            initialDelaySeconds: 30

            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
